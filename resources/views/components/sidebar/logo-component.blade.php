<div class="logo-box">
    <a class="logo logo-light" href="{{ authRoute('user.dashboard') }}">
        <span class="logo-sm">
            <img src="{{ asset($logoSm) }}" alt="">
        </span>
        <span class="logo-lg">
            <img src="{{ asset($logoLight) }}" alt="">
        </span>
    </a>

    <a class="logo logo-dark" href="{{ authRoute('user.dashboard') }}">
        <span class="logo-sm">
            <img src="{{ asset($logoSm) }}" alt="">
        </span>
        <span class="logo-lg">
            <img src="{{ asset($logoDark) }}" alt="">
        </span>
    </a>
</div>
